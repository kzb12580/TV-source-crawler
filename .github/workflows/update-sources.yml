name: Update Sources

on:
  schedule:
    - cron: '0 2 * * 1'
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  update:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: true
        
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          
      - name: Install dependencies
        run: |
          pip install requests
          
      - name: Run crawler
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          STRICTNESS: conservative
        run: python crawler.py
        
      - name: Get stats
        id: stats
        run: |
          TOTAL=$(python3 -c "import json; d=json.load(open('sources.json')); print(d.get('total_sources', 0))")
          AVAILABLE=$(python3 -c "import json; d=json.load(open('sources.json')); print(d.get('total_available', 0))")
          echo "total=$TOTAL" >> $GITHUB_OUTPUT
          echo "available=$AVAILABLE" >> $GITHUB_OUTPUT
          echo "Total: $TOTAL, Available: $AVAILABLE"
          
      - name: Commit changes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git add sources.json sources.compact.json sources.base58.txt sources.adult.json sources.adult.compact.json sources.adult.base58.txt STATS.md failed_sources.json manual_sources.json crawler.py .github/workflows/update-sources.yml
          git diff --quiet && git diff --staged --quiet || git commit -m "Update sources - $(date '+%Y-%m-%d')"
          git push
